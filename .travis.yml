language: python
python:
    - "3.3"
    - "3.4"
    - "3.5"
    - "pypy"
install:
    - "pip install flake8"
    - "pip install -r requirements.txt"
script:
    - "pip install ."
    - "make lint test"
matrix:
    allow_failures:
        - python: "pypy"
